<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="games.sourdough.chalk2">

    <application
        android:name=".Chalk2Application"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher_chalk"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_chalk_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chalk2">
        <activity
            android:name=".workoutconfiguration.WorkoutConfigurationView"
            android:exported="true">
        </activity>
        <activity
            android:name=".workoutcalculator.WorkoutCalculatorView"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>